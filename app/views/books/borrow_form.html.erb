<%= form_for @book, :url => books_borrow_path, :method => "post"  do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book_request from being saved:</h2>
      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= hidden_field_tag 'book_id', @book.id %>
  <div class="field">
    <%= f.label :loan_period %><br>
    <%= f.number_field :loan_period %>
  </div>
  <div class="actions">
    <%= f.submit "borrow" %>
  </div>
<% end %>
